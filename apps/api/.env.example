# Local (Prisma resolve o caminho a partir da pasta prisma/)
DATABASE_URL="file:./dev.db"

# Railway / Docker (caminho absoluto gravável; monte volume em /data para persistir)
# DATABASE_URL="file:/data/dev.db"

PORT=4000
CORS_ORIGIN="http://localhost:3000,http://localhost:3001"

# Railway (demo): aceita https://*.up.railway.app sem listar cada URL no CORS_ORIGIN
# CORS_ALLOW_RAILWAY_APP=1

# Popular cardápio na nuvem (uma vez): GET /setup/seed?key=SUA_CHAVE
# SETUP_KEY=troque-por-uma-senha
